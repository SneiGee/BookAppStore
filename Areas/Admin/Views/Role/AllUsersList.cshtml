@model UserInRoleModel
@{
    ViewData["Title"] = "Manage Users - Administrator";
}

<div class="container">
    <div class="row p-o m-o">
        <div class="col-6">
            <h2 class="text-secondary">Users List</h2>
        </div>
        <div class="col-3 offset-3">
            <a asp-action="AddRole" asp-controller="Role" class="btn btn-info form-control text-white">
                Add New User
            </a>
        </div>
        <div class="col-12 border p-3">

            @if (ViewBag.IsSuccess == true)
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    New User is added successfully. <br />
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            }

            <table id="loadUserData" class="table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Email Address</th>
                        <th>Role Name</th>
                        <th>Action</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>
