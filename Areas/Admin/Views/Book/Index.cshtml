@model AllBooksModel
@{
    ViewData["Title"] = "Manage Books - Administrator";
}

<div class="container">
    <div class="row p-o m-o">
        <div class="col-6">
            <h2 class="text-secondary">Books List</h2>
        </div>
        <div class="col-3 offset-3">
            <a asp-action="AddRole" asp-controller="Role" class="btn btn-info form-control text-white">
                Add New Book
            </a>
        </div>
        <div class="col-12 border p-3">

            @if (ViewBag.IsSuccess == true)
            {
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    New book is added successfully. <br />
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
            }

            <table id="loadBooksData" class="table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th>Book Name</th>
                        <th>Category</th>
                        <th>Author</th>
                        <th>Total Page</th>
                        <th>Action</th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>
